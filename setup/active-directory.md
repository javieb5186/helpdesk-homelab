## Troubleshooting Notes

### DNS Warning During Setup
After assigning the domain controller a static IP with loopback DNS (127.0.0.1), Windows temporarily displayed "No Internet Access."

This occurred because DNS services were not yet installed. Connectivity was verified successfully using ping tests.
